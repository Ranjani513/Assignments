package com.nisum.main;

import java.util.List;
import java.util.stream.Collectors;

public class AddressOperation {
public void  filter(List<Address> a)
{
	/*List<Address> res=a.stream().filter(p->p!=null).collect(Collectors.toList());
	return res;*/
	System.out.println("In the Address operation");
}

}
