apiVersion:v1
kind:Deployment
metadata:
   name:nginx-rc
   labels:
     app:App
spec:
  replicas:3
  selector:
  template:
    metadata:
      name:nginx-pod
      label:
        app:nginx
        tier:backend
  spec:
    containers:
    - name:nginxContainer
      image:nginx
  